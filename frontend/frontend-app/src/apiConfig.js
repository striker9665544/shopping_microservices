// src/apiConfig.js
const API_BASE_URLS = {
    LOGIN: 'http://localhost:8081',
    SEARCH: 'http://localhost:9000',
    CART: 'http://localhost:8085'
    // When we add the API Gateway, we'll change this to just one URL.
};

export default API_BASE_URLS;